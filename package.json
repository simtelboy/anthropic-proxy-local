{
  "name": "nvidia-anthropic-proxy",
  "version": "2.0.0",
  "description": "Anthropic API compatible proxy for local inference servers (Ollama, LM Studio, llama.cpp, etc.)",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.js",
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}